# Mineo 云同步（桌面挂件 ↔ web 端）配置
# 复制为 .env（或 .env.local），填入你的 Supabase 项目信息。
# 两个值都来自 Supabase 控制台 → Project Settings → API。
# anon key 是「前端公开 key」，放前端是预期用法；真正的 service_role key 绝不要放这里。
# 不配置这两项时，应用自动降级为纯本地（同一浏览器内可用），不影响 web 端本身。

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
